/**
 * hold all game specific data
 */
var game = {

    /**
     * object where to store game global scole
     */
    data : {
        gold: {
            balance : 0,
        },
        score : 0
    },

    // a reference to the texture atlas
    texture : null
};

export default game;
